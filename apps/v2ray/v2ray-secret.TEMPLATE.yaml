# Template only - DO NOT commit a real secret here.

# Only the actual client ID is sealed. config.json lives in plaintext in
# v2ray-configmap.yaml, with __V2RAY_ID__ as the placeholder the Deployment's
# initContainer replaces at startup.

# Command:
# uuidgen | tr -d '\n' > ./v2ray-id
# kubectl create secret generic v2ray-id -n v2ray-vpn \
#   --from-file=id=./v2ray-id \
#   --dry-run=client -o yaml | kubeseal --format yaml --controller-name sealed-secrets-controller --controller-namespace kube-system > apps/v2ray/v2ray-secret.sealed.yaml

# Replace REPLACE_ME_V2RAY_ID with a generated client UUID.

apiVersion: v1
kind: Secret
metadata:
  name: v2ray-id
  namespace: v2ray-vpn
type: Opaque
stringData:
  id: |
    REPLACE_ME_V2RAY_ID
