# Template only - DO NOT commit a real secret here.

# Command:
# kubeseal --format yaml --controller-name sealed-secrets-controller --controller-namespace kube-system < apps/v2ray/v2ray-secret.plain.yaml > apps/v2ray/v2ray-secret.sealed.yaml
# Replace REPLACE_ME_V2RAY_ID with a generated client UUID.

apiVersion: v1
kind: Secret
metadata:
  name: v2ray-id
  namespace: v2ray-vpn
type: Opaque
stringData:
  id: |
    REPLACE_ME_V2RAY_ID
