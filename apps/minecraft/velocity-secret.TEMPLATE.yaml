# Template only - DO NOT commit a real secret here.

# kubeseal --format yaml --controller-name sealed-secrets-controller --controller-namespace kube-system < apps/minecraft/velocity-secret.plain.yaml > /tmp/velocity-sealed.yaml
# then copy spec.encryptedData["forwarding.secret"] into apps/minecraft/templates/sealedsecret.yaml

# Replace REPLACE_ME_VELOCITY_FORWARDING_SECRET with a generated Velocity proxy forwarding secret.

apiVersion: v1
kind: Secret
metadata:
  name: velocity-forwarding-secret
  namespace: minecraft
type: Opaque
stringData:
  forwarding.secret: |
    REPLACE_ME_VELOCITY_FORWARDING_SECRET
