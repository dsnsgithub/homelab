apiVersion: v1
kind: Service
metadata:
  name: limbo-service
  labels:
    {{- include "minecraft.labels" . | nindent 4 }}
spec:
  selector:
    app: limbo
  ports:
  - name: limbo
    port: {{ .Values.limbo.servicePort }}
    targetPort: {{ .Values.limbo.containerPort }}
