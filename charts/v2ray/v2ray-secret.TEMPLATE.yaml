# Template only - DO NOT commit a real secret here.

# Command:
# kubeseal --format yaml --controller-name sealed-secrets-controller --controller-namespace kube-system < charts/v2ray/v2ray-secret.plain.yaml > charts/v2ray-secrets/templates/sealedsecret.yaml
#
# The output replaces that file verbatim (it is deployed as-is to prod only;
# staging gets dummy secrets from charts/staging-secrets instead).

# Replace REPLACE_ME_V2RAY_ID a generated client UUID.

apiVersion: v1
kind: Secret
metadata:
  name: v2ray-config
  namespace: v2ray-vpn
type: Opaque
stringData:
  config.json: |
    {
      "dns": {
        "servers": [
          "https://dns.adguard.com"
        ]
      },
      "inbounds": [
        {
          "port": 10086,
          "protocol": "vmess",
          "settings": {
            "clients": [
              {
                "alterId": 64,
                "id": "REPLACE_ME_V2RAY_ID"
              }
            ]
          },
          "streamSettings": {
            "network": "ws",
            "security": "none",
            "wsSettings": {
              "path": "/dsns"
            }
          }
        }
      ],
      "outbounds": [
        {
          "protocol": "freedom",
          "settings": {}
        }
      ]
    }