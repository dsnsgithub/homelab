{{- range .Values.traefikServices }}
---
apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.namespace }}
spec:
  loadBalancer:
    servers:
      - url: {{ .url }}
{{- end }}