apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Traefik now lives in namespace `traefik` (ArgoCD Helm release), so the
# Cloudflare token must be sealed for THAT namespace. The old kube-system
# sealed secret is gone; recreate it (see cloudflare-secret.TEMPLATE.yaml),
# then uncomment below:
resources: []
  # - cloudflare-secret.sealed.yaml
